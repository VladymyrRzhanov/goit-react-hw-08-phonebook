import { Title, Subtitle, HeroContainer, MainSection } from "./styles";
const Hero = () => (
    <MainSection>

    <HeroContainer>
        <Title>
            Manage information, rule the world!
        </Title>
        <Subtitle>
            Click. Create. Use it.
        </Subtitle>
    </HeroContainer>
    </MainSection>
);

export default Hero;