import { Title, Subtitle, HeroContainer } from "./styles";
const Hero = () => (
    <HeroContainer>
        <Title>
            Manage information, rule the world!
        </Title>
        <Subtitle>
            Click. Create. Use it.
        </Subtitle>
    </HeroContainer>
);

export default Hero;